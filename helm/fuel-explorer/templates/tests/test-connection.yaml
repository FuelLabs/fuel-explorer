apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fuel-explorer.fullname" . }}-test-connection"
  labels:
    {{- include "fuel-explorer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fuel-explorer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
