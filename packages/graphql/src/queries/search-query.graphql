query searchQuery($search: String!) {
  account(address: $search) {
    address
    transactions {
      id
    }
  }
  contractId(id: $search) {
    id
  }
  blockInfo(id: $search) {
    id
    height
  }
  transactionId(id: $search) {
    id
  }
}
