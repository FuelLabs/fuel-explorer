fragment BlockItem on Block {
  # Custom Resolvers
  time {
    fromNow
    full
    rawTai64
    rawUnix
  }

  # Block Data
  id
  header {
    transactionsCount
    time
  }
  transactions {
    id
  }
}

query getBlock($height: U64!) {
  block(height: $height) {
    ...BlockItem
  }
}
