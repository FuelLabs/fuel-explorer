query getBlocksDashboard {
  getBlocksDashboard {
    nodes {
      timestamp
      gasUsed
      gasUsedInUsd
      totalFee
      totalFeeInUsd
      blockNo
      producer
      blockHash
    }
  }
}
