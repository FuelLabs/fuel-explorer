query search($query: String!) {
  search(query: $query) {
    account {
      address
      transactions {
        id
      }
    }
    block {
      height
      id
    }
    contract {
      id
    }
    transaction {
      id
    }
  }
}
