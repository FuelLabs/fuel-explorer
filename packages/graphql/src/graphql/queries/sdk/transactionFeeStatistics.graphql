query transactionFeeStatistics($timeFilter: String){
    transactionFeeStatistics(timeFilter: $timeFilter){
        nodes{
            tx_count
            time
            fee_spent
        }
    }
}