query cumulativeAccountStatistics($timeFilter: String!) {
    cumulativeAccountStatistics(timeFilter: $timeFilter) {
        nodes {
            cumulativeAccounts
            timestamp
        }
    }
}