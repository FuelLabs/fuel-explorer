query dailyActiveAccountStatistics($timeFilter: String!) {
    dailyActiveAccountStatistics(timeFilter: $timeFilter) {
        nodes {
            activeAccounts
            timestamp
        }
    }
}
